version: '3.8'

services:
  threat_aggregator:
    build: .
    container_name: threat_aggregator
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    restart: unless-stopped
